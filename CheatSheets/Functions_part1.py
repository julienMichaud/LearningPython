return exit the function


def exponent(num, power=2):  ## second parameter has a default value of 2 if non is specified when calling the function
    return num ** power

print(exponent(2,3)) #8
print(exponent(3)) # 9

to use a global function, use global num par example inside the def of the function